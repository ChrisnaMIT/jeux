{% extends 'base.html.twig' %}

{% block title %}Hello PostController!{% endblock %}

{% block body %}

    {% for post in posts %}

        <div class="card">
            <h2 class="card-title">{{ post.title }}</h2>
            <div class="card-body">
                {% if app.user %}
                    <p>Tu as écrit ce post</p>
                    {% if post.author == app.user %}



                    {% endif %}
                {% endif %}
                <p>author : {{ post.author.email }}</p>
                <p>Le commentaire: {{ post.content }}</p>
                <a href="{{ path('app_post_show', {id: post.id}) }}" class="btn btn-primary">Voir ce post</a>

            </div>
        </div>

    {% endfor %}









{% endblock %}
